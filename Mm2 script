--[[
    SPECTRE X - MM2 Script
    Версия: 2.0 (исправленная)
    Функции: Silent Aim (только убийца) + Wallcheck, ESP (сквозь стены), 
    Chams, Auto Shoot, Auto Farm
    UI: Rayfield с круглой кнопкой открытия
--]]

-- === ЗАГРУЗКА RAYFIELD ===
if not game:IsLoaded() then game.Loaded:Wait() end

local Rayfield = nil
if not getgenv().Rayfield then
    local RayfieldLib = loadstring(game:HttpGet('https://raw.githubusercontent.com/shlexware/Rayfield/main/source'))()
    getgenv().Rayfield = RayfieldLib
    Rayfield = RayfieldLib
else
    Rayfield = getgenv().Rayfield
end

-- === ОСНОВНЫЕ СЕРВИСЫ ===
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- === СОСТОЯНИЯ ===
local silentAim = false
local wallcheck = false
local autoShoot = false
local espEnabled = false
local chamsEnabled = false
local farmEnabled = false

-- === REMOTE СОБЫТИЯ ===
local shootRemote = ReplicatedStorage:FindFirstChild("Shoot") or ReplicatedStorage:FindFirstChild("Fire")
local collectCoinRemote = ReplicatedStorage:FindFirstChild("CollectCoin") or ReplicatedStorage:FindFirstChild("CoinCollected")

-- === ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ ===
local function getPlayerRole(player)
    if player.Character then
        for _, tool in pairs(player.Character:GetChildren()) do
            if tool:IsA("Tool") then
                local name = tool.Name:lower()
                if name:find("knife") or name:find("dagger") then
                    return "Murderer"
                elseif name:find("gun") or name:find("pistol") then
                    return "Sheriff"
                end
            end
        end
    end
    return "Innocent"
end

local function isVisible(targetPart)
    if not wallcheck then return true end
    local origin = Camera.CFrame.Position
    local direction = (targetPart.Position - origin).Unit * 500
    local raycastParams = RaycastParams.new()
    raycastParams.FilterType = Enum.RaycastFilterType.Blacklist
    raycastParams.FilterDescendantsInstances = {LocalPlayer.Character}
    local result = workspace:Raycast(origin, direction, raycastParams)
    if result then
        local hit = result.Instance
        if hit and hit:IsDescendantOf(targetPart.Parent) then
            return true
        end
    end
    return false
end

local function getMurderer()
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Head") then
            if getPlayerRole(player) == "Murderer" then
                return player
            end
        end
    end
    return nil
end

-- === СОЗДАНИЕ КРУГЛОЙ КНОПКИ ===
local menuVisible = true

local function createToggleButton()
    local button = Instance.new("ImageButton")
    button.Name = "SpectreToggle"
    button.Parent = LocalPlayer:WaitForChild("PlayerGui")
    button.Size = UDim2.new(0, 50, 0, 50)
    button.Position = UDim2.new(0.02, 0, 0.15, 0)
    button.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
    button.BackgroundTransparency = 0.2
    button.BorderSizePixel = 0
    button.Image = "rbxassetid://4483362458"
    button.ImageColor3 = Color3.fromRGB(255, 255, 255)
    button.ImageTransparency = 0.3
    button.ScaleType = Enum.ScaleType.Fit
    
    -- Создаём скругление
    local corner = Instance.new("UICorner")
    corner.Parent = button
    corner.CornerRadius = UDim.new(1, 0)
    
    -- Обводка
    local stroke = Instance.new("UIStroke")
    stroke.Parent = button
    stroke.Color = Color3.fromRGB(100, 100, 255)
    stroke.Thickness = 2
    stroke.Transparency = 0.5
    
    -- Эффект свечения
    local glow = Instance.new("ImageLabel")
    glow.Parent = button
    glow.Size = UDim2.new(1.4, 0, 1.4, 0)
    glow.Position = UDim2.new(-0.2, 0, -0.2, 0)
    glow.BackgroundTransparency = 1
    glow.Image = "rbxassetid://4483362458"
    glow.ImageColor3 = Color3.fromRGB(100, 100, 255)
    glow.ImageTransparency = 0.8
    glow.ScaleType = Enum.ScaleType.Fit
    
    -- Анимация пульсации
    task.spawn(function()
        while true do
            for i = 0.5, 1, 0.1 do
                task.wait(0.05)
                glow.ImageTransparency = 0.8 - (i * 0.5)
            end
            for i = 1, 0.5, -0.1 do
                task.wait(0.05)
                glow.ImageTransparency = 0.8 - (i * 0.5)
            end
        end
    end)
    
    return button
end

local toggleButton = createToggleButton()

-- === СОЗДАНИЕ GUI (RAYFIELD) ===
local Window = Rayfield:CreateWindow({
    Name = "Spectre X",
    Icon = 0,
    LoadingTitle = "Spectre X",
    LoadingSubtitle = "v2.0",
    Theme = "Dark",
    DisableBlur = false,
    Keybind = Enum.KeyCode.RightControl,
})

-- Обработчик кнопки
toggleButton.MouseButton1Click:Connect(function()
    menuVisible = not menuVisible
    if menuVisible then
        Window:Show()
        toggleButton.ImageTransparency = 0.3
    else
        Window:Hide()
        toggleButton.ImageTransparency = 0.8
    end
end)

-- === ВКЛАДКА AIMBOT ===
local AimbotTab = Window:CreateTab("Aimbot", 4483362458)

AimbotTab:CreateToggle({
    Name = "Silent Aim (только убийца)",
    CurrentValue = false,
    Flag = "SilentAim",
    Callback = function(state)
        silentAim = state
    end,
})

AimbotTab:CreateToggle({
    Name = "Wallcheck (проверка стен)",
    CurrentValue = false,
    Flag = "Wallcheck",
    Callback = function(state)
        wallcheck = state
    end,
})

AimbotTab:CreateToggle({
    Name = "Auto Shoot",
    CurrentValue = false,
    Flag = "AutoShoot",
    Callback = function(state)
        autoShoot = state
    end,
})

AimbotTab:CreateSlider({
    Name = "Auto Shoot Distance",
    Range = {50, 400},
    Increment = 10,
    Suffix = "px",
    CurrentValue = 200,
    Flag = "ShootDistance",
    Callback = function(value)
        shootDistance = value
    end,
})

-- === ВКЛАДКА VISUALS ===
local VisualsTab = Window:CreateTab("Visuals", 4483362458)

VisualsTab:CreateToggle({
    Name = "ESP (сквозь стены)",
    CurrentValue = false,
    Flag = "ESP",
    Callback = function(state)
        espEnabled = state
        if state then
            updateESP()
        else
            clearESP()
        end
    end,
})

VisualsTab:CreateToggle({
    Name = "Chams (подсветка)",
    CurrentValue = false,
    Flag = "Chams",
    Callback = function(state)
        chamsEnabled = state
        if state then
            updateChams()
        else
            clearChams()
        end
    end,
})

-- === ВКЛАДКА MISC ===
local MiscTab = Window:CreateTab("Misc", 4483362458)

MiscTab:CreateToggle({
    Name = "Auto Farm (монеты)",
    CurrentValue = false,
    Flag = "AutoFarm",
    Callback = function(state)
        farmEnabled = state
    end,
})

-- === ESP И CHAMS ФУНКЦИИ ===
local espObjects = {}
local chamsObjects = {}
local shootDistance = 200

function updateESP()
    if not espEnabled then return end
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Head") then
            local head = player.Character.Head
            if not head:FindFirstChild("ESP_Billboard") then
                local bill = Instance.new("BillboardGui")
                bill.Name = "ESP_Billboard"
                bill.Parent = head
                bill.Size = UDim2.new(0, 120, 0, 50)
                bill.Adornee = head
                bill.AlwaysOnTop = true
                bill.StudsOffset = Vector3.new(0, 1.5, 0)
                bill.MaxDistance = 500

                local label = Instance.new("TextLabel")
                label.Parent = bill
                label.Size = UDim2.new(1, 0, 0.6, 0)
                label.BackgroundTransparency = 1
                label.Text = player.Name
                label.TextScaled = true
                label.Font = Enum.Font.GothamBold
                label.TextColor3 = Color3.fromRGB(255, 255, 255)
                label.TextStrokeTransparency = 0.3

                local roleLabel = Instance.new("TextLabel")
                roleLabel.Parent = bill
                roleLabel.Size = UDim2.new(1, 0, 0.4, 0)
                roleLabel.Position = UDim2.new(0, 0, 0.6, 0)
                roleLabel.BackgroundTransparency = 1
                roleLabel.Text = getPlayerRole(player)
                roleLabel.TextScaled = true
                roleLabel.Font = Enum.Font.Gotham
                roleLabel.TextColor3 = Color3.fromRGB(200, 200, 200)

                espObjects[player] = bill
            end
            -- Обновляем цвет
            local role = getPlayerRole(player)
            local color = role == "Murderer" and Color3.fromRGB(255, 0, 0) or
                         role == "Sheriff" and Color3.fromRGB(0, 100, 255) or
                         Color3.fromRGB(0, 255, 0)
            local label = espObjects[player]:FindFirstChild("TextLabel")
            if label then label.TextColor3 = color end
        end
    end
end

function clearESP()
    for player, bill in pairs(espObjects) do
        if bill and bill.Parent then bill:Destroy() end
    end
    espObjects = {}
end

function updateChams()
    if not chamsEnabled then return end
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            local char = player.Character
            if not char:FindFirstChild("ChamsHighlight") then
                local hl = Instance.new("Highlight")
                hl.Name = "ChamsHighlight"
                hl.Parent = char
                hl.Adornee = char
                hl.FillColor = Color3.fromRGB(255, 255, 255)
                hl.FillTransparency = 0.6
                hl.OutlineColor = Color3.fromRGB(255, 255, 255)
                hl.OutlineTransparency = 0.2
                hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                chamsObjects[player] = hl
            end
            local role = getPlayerRole(player)
            local color = role == "Murderer" and Color3.fromRGB(255, 0, 0) or
                         role == "Sheriff" and Color3.fromRGB(0, 100, 255) or
                         Color3.fromRGB(0, 255, 0)
            local hl = chamsObjects[player]
            hl.FillColor = color
            hl.OutlineColor = color
        end
    end
end

function clearChams()
    for player, hl in pairs(chamsObjects) do
        if hl and hl.Parent then hl:Destroy() end
    end
    chamsObjects = {}
end

-- === ОБРАБОТКА ИГРОКОВ ===
Players.PlayerAdded:Connect(function(player)
    player.CharacterAdded:Connect(function()
        task.wait(0.5)
        if espEnabled then updateESP() end
        if chamsEnabled then updateChams() end
    end)
end)

Players.PlayerRemoving:Connect(function(player)
    if espObjects[player] then 
        espObjects[player]:Destroy() 
        espObjects[player] = nil 
    end
    if chamsObjects[player] then 
        chamsObjects[player]:Destroy() 
        chamsObjects[player] = nil 
    end
end)

-- === ОСНОВНОЙ ЦИКЛ ===
RunService.RenderStepped:Connect(function()
    -- Silent Aim
    if silentAim then
        local murderer = getMurderer()
        if murderer and murderer.Character and murderer.Character:FindFirstChild("Head") then
            local head = murderer.Character.Head
            if isVisible(head) then
                Camera.CFrame = CFrame.new(Camera.CFrame.Position, head.Position)
            end
        end
    end

    -- Auto Shoot
    if autoShoot then
        local murderer = getMurderer()
        if murderer and murderer.Character and murderer.Character:FindFirstChild("Head") then
            local head = murderer.Character.Head
            if isVisible(head) then
                local screenPoint, onScreen = Camera:WorldToScreenPoint(head.Position)
                if onScreen then
                    local mousePos = UserInputService:GetMouseLocation()
                    local dist = (Vector2.new(screenPoint.X, screenPoint.Y) - mousePos).Magnitude
                    if dist < shootDistance then
                        if shootRemote then
                            shootRemote:FireServer(head.Position)
                        end
                    end
                end
            end
        end
    end

    -- Auto Farm
    if farmEnabled then
        for _, coin in pairs(workspace:GetChildren()) do
            if coin:IsA("Part") and coin.Name:lower():find("coin") then
                if collectCoinRemote then
                    collectCoinRemote:FireServer(coin)
                end
            end
        end
    end
end)

-- === ПЕРИОДИЧЕСКОЕ ОБНОВЛЕНИЕ ===
task.spawn(function()
    while true do
        task.wait(0.5)
        if espEnabled then updateESP() end
        if chamsEnabled then updateChams() end
    end
end)

print("Spectre X v2.0 loaded!")
print("Нажмите на круглую кнопку в левом верхнем углу, чтобы открыть/закрыть меню")
print("Или нажмите RightControl")
